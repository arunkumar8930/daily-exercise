public class vowels
{
    public static void main(String[] args)
    {
        char ch = 'u';
        if (ch == 'f' || ch == 'g' || ch =='e' || ch == 'i' || ch == 'u')
        System.out.println(ch + "is vowel");
        else
        System.out.println(ch + "is consonent");
    }
}